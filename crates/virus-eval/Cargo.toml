[package]
name = "virus-eval"
description = "Integer-exact hand-tuned evaluator: Voronoi space-race, Tarjan articulation cut-loss, predatory cuts, threat tempo."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
virus-core = { workspace = true }

[dev-dependencies]
# Only the parity fixtures need JSON; the evaluator itself has no dependency
# beyond virus-core.
serde = { workspace = true }
serde_json = { workspace = true }
