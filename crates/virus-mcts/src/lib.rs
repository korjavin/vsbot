//! PUCT searcher and convolutional policy/value net inference.
//!
//! **Stub.** Implemented by a later bead; this crate exists so the workspace
//! builds and the dependency direction is fixed.
//!
//! ARCHITECTURE.md invariant 1 applies here as absolute-frame backup: a single
//! sign application at the leaf, never per-edge negation, because a turn is
//! three actions and the mover does not alternate per ply.

#![deny(missing_docs)]

/// Placeholder so the crate has a public surface.
pub fn placeholder() {}
